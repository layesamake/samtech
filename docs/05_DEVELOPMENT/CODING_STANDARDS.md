# Standards de code

Dart formaté automatiquement, analyse statique stricte, noms explicites, fonctions courtes, immutabilité privilégiée, erreurs typées, dépendances injectées, documentation des API publiques et zéro secret dans le dépôt.

