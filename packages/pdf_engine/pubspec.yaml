name: samtech_pdf_engine
description: Contrats du moteur de documents PDF SAMTECH.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2
resolution: workspace

dev_dependencies:
  flutter_lints: ^6.0.0
