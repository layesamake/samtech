name: samtech_workspace
description: Workspace racine de la plateforme logicielle SAMTECH.
publish_to: none
version: 0.1.0

environment:
  sdk: '>=3.5.0 <4.0.0'

dev_dependencies:
  melos: ^6.3.0

melos:
  packages:
    - apps/**
    - packages/**
